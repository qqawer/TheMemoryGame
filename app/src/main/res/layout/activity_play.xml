<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    android:background="#0B0A0D"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 🎮 Game HUD (replaces Toolbar + Timer + Matches) -->
    <include
        android:id="@+id/hudGame"
        layout="@layout/include_hud_game"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- 🃏 Card area -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvCards"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="8dp"
        android:clipToPadding="false"
        android:clipChildren="false"
        android:overScrollMode="never"
        android:background="@drawable/bg_table_surface"
        app:layout_constraintTop_toBottomOf="@id/hudGame"
        app:layout_constraintBottom_toTopOf="@id/adSpacer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <!-- ✅ Bottom placeholder: height = ad height + margin, ensuring RV never draws into the ad area -->
    <View
        android:id="@+id/adSpacer"
        android:layout_width="0dp"
        android:layout_height="136dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <!-- 📢 Bottom ad bar -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/adContainer"
        android:layout_width="0dp"
        android:layout_height="112dp"
        android:layout_margin="12dp"
        android:elevation="1dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="1dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent">

        <androidx.viewpager2.widget.ViewPager2
            android:id="@+id/vpAds"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"/>
    </com.google.android.material.card.MaterialCardView>




</androidx.constraintlayout.widget.ConstraintLayout>

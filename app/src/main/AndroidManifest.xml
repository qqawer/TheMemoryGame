<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml - Complete Configuration for Memory Game
    
    Updated for English version code with MainActivity as entry point
    
    File path:
    app/src/main/AndroidManifest.xml
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions - Required for API communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheMemoryGame"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- ✅ MainActivity - Main Menu/Game Main Screen (LAUNCHER - App Entry Point) -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ✅ LoginActivity - Login Screen (Launched from MainActivity) -->
        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Login"
            android:parentActivityName=".activities.MainActivity">
            <!-- Support for Up navigation -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <!-- PlayActivity - Game Screen (Member 4 - To be implemented) -->
        <activity
            android:name=".PlayActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Game" />

        <!-- TODO: Add LeaderboardActivity when Member 5 implements it -->
        <!--
        <activity
            android:name=".activities.LeaderboardActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Leaderboard" />
        -->

        <!-- TODO: Add other activities as needed by team members -->
        <!-- Example: FetchActivity for image downloading (Member 3) -->
        <!--
        <activity
            android:name=".activities.FetchActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:label="Download Images" />
        -->

    </application>

</manifest>
